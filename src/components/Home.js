import React from "react";
import {
  Container, Header,
} from "semantic-ui-react";

const Home = () => (
  <Container text style={{ marginTop: "2rem" }}>
    <Header>Welcome to Medica</Header>
  </Container>
);

export default Home;
